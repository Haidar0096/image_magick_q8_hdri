# Run with `flutter pub run ffigen --config ffigen.yaml`.
name: ImageMagickQ8HdriBindings
description: |
  Bindings for `src/image_magick_q8_hdri.h`.

  Regenerate bindings with `flutter pub run ffigen --config ffigen.yaml`.
output: 'lib/image_magick_q8_hdri_bindings_generated.dart'
headers:
  entry-points:
    - 'src/image_magick_q8_hdri.h'
  include-directives:
    - 'src/image_magick_q8_hdri.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
